{
  "buildCommand": "cd dashboard && npm install && npx prisma generate && npx prisma migrate deploy && npm run build",
  "outputDirectory": "dashboard/.next",
  "installCommand": "cd dashboard && npm install"
}
